# VANRYUSDT open-interest operational disposition

The August v6 engineering-shadow run retained `VANRYUSDT` in the frozen
August roster. All four open-interest polls produced `POLL_GAP` records. A
public Binance UM request made during the audit returned HTTP 400 with code
`-4108` and message `Symbol is on delivering or delivered or settling or
closed or pre-trading.` The same public `exchangeInfo` response reported
`status: SETTLING` for `VANRYUSDT`.

This is a structural primary-source-unavailable condition, not an imputation
or rank substitution. The roster is unchanged; the H02 primary-source
observation is ineligible for each affected cycle. Other hypotheses and
symbols are unaffected.
