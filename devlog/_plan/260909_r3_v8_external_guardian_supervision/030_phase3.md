# WP2 — OS registration and synthetic qualification

Create a sanitized one-shot Task Scheduler registration path for R3-v8-Guardian-Supervisor, actioning only the supervisor, cadence every 5 minutes, MultipleInstancesPolicy=IgnoreNew, per-user Interactive/Limited principal, no credentials in arguments. Attempt registration once; if denied record TASK_SCHEDULER_REGISTRATION_BLOCKED_PERMISSION without retry. Verify actual task/action/trigger/repetition/principal/LastTaskResult if registered. Qualify at least 20 cases: healthy no-op, absent guardian relaunch once, duplicate guardian, active/stale/malformed guardian lock, active/stale/duplicate collector lock, chain/seal failure, source/registry/roster drift, expired policy, concurrent/repeated invocation, reboot-style dead PID, and proof supervisor never launches collector or mints child authorization. Record SAFE_SKIP_DESTRUCTIVE_REBOOT_TEST; do not reboot or kill live processes.