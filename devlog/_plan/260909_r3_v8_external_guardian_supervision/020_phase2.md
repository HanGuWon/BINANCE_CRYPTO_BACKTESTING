# WP1 — One-shot operations-only supervisor

Add ops/r3/ensure_r3_v8_guardian.py. One invocation exits after one decision: NO_ACTION_LIVE, RELAUNCH_GUARDIAN, or fail-closed BLOCK_*. Acquire a supervisor lock in a domain distinct from guardian and collector locks; concurrent invocation must no-op/block safely. Validate exact implementation/source/registry/roster/manifest/seal and unexpired standing policy before action. Inspect process command lines and lock contents. Archive stale guardian/collector locks only after proving recorded PID dead, no matching command line/candidate, valid parse, exact identity, chain PASS, and SEALED seal; SHA-256, copy, fsync, reread/hash, receipt, then remove. Relaunch only run_r3_v8_guardian.ps1 -Persistent -PollSeconds 300; never call launch_r3_v8_resume.ps1, mint child auth, or write scientific data. Add focused tests for authority separation, lock ambiguity, identity drift, expiry, and decisions.