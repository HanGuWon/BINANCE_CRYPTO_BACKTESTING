# R3 p4 — outcome-blind engineering pilot and launch gate

Run only a real public-data pilot for fixed BTCUSDT and ETHUSDT under the
D-backed R3 root. Exercise REST envelopes, closed 15m klines, response headers,
append-only raw files, hash-linked manifests, health receipts, lock/restart
behavior, and storage counts. Pilot identity is engineering context and cannot
enter scientific outcomes. No return, PnL, signal ranking, threshold tuning,
historical holdout, or R2B2 access is permitted.

Verification: pilot receipt contains only operational counts/bytes/latency/gap
fields; rerun targeted R3 tests and full pytest; final manifest remains blocked
unless every launch gate is evidenced.
