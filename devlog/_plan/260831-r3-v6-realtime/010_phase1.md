# Phase 1 — sealed state and v5 distinction

Read-only checks: branch/HEAD/origin synchronization, registry and August
roster hashes/replay, semantic-parity receipt, scientific-source cleanliness,
and explicit NOT_STARTED/UNTOUCHED markers for outcomes, holdout, and R2B2.

Append-only documentation must state that `engineering_shadow_august_v5_retry`
used real Binance requests and persistent collector/forceOrder architecture,
but its four absolute logical boundaries were deliberately injected in the
past with `wait_for_boundary=False`; it is valid architecture/path
qualification, not wall-clock spacing evidence. Record the unchanged v5 tree
hash before and after the clarification.

Verification: hash commands, roster replay command, and a diff proving no v5
root content changed.
